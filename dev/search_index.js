var documenterSearchIndex = {"docs":
[{"category":"section","location":"#TarFS.jl","page":"Home","text":"An in-memory filesystem backed by a .tar.gz archive: load it once, read files by path, stage new or modified files, and flush everything back to a gzip-compressed POSIX tarball — atomically and reproducibly. See the repository README for motivation and design notes; this page documents the API.","title":"TarFS.jl"},{"category":"section","location":"#Usage","page":"Home","text":"Reading — readfile returns raw bytes as a String, which composes with anything that accepts an IOBuffer:\n\nusing TarFS\n\nfs = InMemoryFileSystem(\"data.tar.gz\")\ncsv = readfile(fs, \"data/p175.csv\")\n\nWriting, including atomic read-modify-write of an archive in place:\n\nopen_tarball_gz(\"data.tar.gz\"; from = \"data.tar.gz\") do fs\n    writefile(fs, \"meta/run.txt\", \"seed = 42\\n\")\nend","title":"Usage"},{"category":"section","location":"#Public-API","page":"Home","text":"Nothing is exported; qualify names with TarFS..","title":"Public API"},{"category":"section","location":"#Guarantees-and-limits","page":"Home","text":"Flushes are atomic against application failure (temporary file plus same-directory rename; no fsync, so power-loss durability is the filesystem's department). Output bytes are reproducible: entries are written in sorted path order with normalized metadata, matching Tar.jl's conventions. Reads are lazy and cached; flushing streams source-backed entries without caching them. The in-memory writer handles regular files only, with USTAR path-length limits and an 8 GiB per-entry cap — write_tarball_gz_viatar lifts the path restriction at the cost of a disk round-trip. Filesystem instances are not thread-safe.","title":"Guarantees and limits"},{"category":"section","location":"#Docstring-index","page":"Home","text":"","title":"Docstring index"},{"category":"type","location":"#TarFS.InMemoryFileSystem","page":"Home","text":"InMemoryFileSystem\n\nAn in-memory filesystem backed by a .tar.gz archive. Construct it empty with InMemoryFileSystem() or from an archive with InMemoryFileSystem(path); read entries with readfile, stage changes with writefile, and flush with write_tarball_gz or open_tarball_gz.\n\n\n\n\n\n","title":"TarFS.InMemoryFileSystem"},{"category":"function","location":"#TarFS.readfile","page":"Home","text":"readfile(fs, path) -> String\n\nContents of path as raw bytes in a String, decoded from the in-memory archive on first access and cached thereafter. Throws KeyError for unknown paths.\n\n\n\n\n\n","title":"TarFS.readfile"},{"category":"function","location":"#TarFS.writefile","page":"Home","text":"writefile(fs::InMemoryFileSystem, path::AbstractString, content::Union{AbstractString, AbstractVector{UInt8}})\n\nStage (or overwrite) a file. content may be a string or a byte vector; byte vectors are copied, so the caller's buffer is left intact. Overwriting a source-backed path replaces the entry, so read -> transform -> writefile is read-modify-write.\n\n\n\n\n\n","title":"TarFS.writefile"},{"category":"function","location":"#TarFS.deletefile","page":"Home","text":"deletefile(fs, path) -> Nothing\n\nRemove path from the filesystem; a no-op if absent.\n\n\n\n\n\n","title":"TarFS.deletefile"},{"category":"function","location":"#TarFS.open_tarball_gz","page":"Home","text":"open_tarball_gz(f, path; from = nothing)\n\nRun f(fs) on a writable filesystem, then flush it to path. Pass from = \"in.tar.gz\" to seed from an existing archive; from == path is fine — the source is fully loaded up front and the write is atomic. The flush happens only if f returns normally.\n\n\n\n\n\n","title":"TarFS.open_tarball_gz"},{"category":"function","location":"#TarFS.write_tarball_gz","page":"Home","text":"write_tarball_gz(fs::InMemoryFileSystem, path::AbstractString)\n\nFlush the filesystem to a gzip-compressed tarball at path. Atomic: a failure mid-write leaves any existing archive at path untouched, so seeding from = path (read-modify-write) cannot destroy the source.\n\n\n\n\n\n","title":"TarFS.write_tarball_gz"},{"category":"function","location":"#TarFS.write_tarball","page":"Home","text":"write_tarball(fs::InMemoryFileSystem, io::IO)\n\nWrite the whole filesystem as a POSIX tarball to io (uncompressed). Source-backed files are streamed, not cached; fs.tarball_io's position is clobbered, which is fine — readfile always seeks first.\n\n\n\n\n\n","title":"TarFS.write_tarball"},{"category":"function","location":"#TarFS.write_tarball_gz_viatar","page":"Home","text":"write_tarball_gz_viatar(fs, path)\n\nFlush to path via a temporary directory and Tar.create: slower than write_tarball_gz, but supports arbitrarily long member paths and emits explicit directory entries. Same atomic temp-file + rename discipline.\n\n\n\n\n\n","title":"TarFS.write_tarball_gz_viatar"}]
}
